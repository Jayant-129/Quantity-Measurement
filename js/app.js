/**
 * app.js
 * @author - developer
 * @version - 1.0
 */
document.addEventListener('DOMContentLoaded', async () => {
    const state = { type: 'Length', action: 'Conversion', fromUnit: '', toUnit: '', operator: '+' };
});
